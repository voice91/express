/**
 * This file is generated by Appinvento, also it can be overwritten by Appinvento.
 * Only fields name will be overwritten, if the field name will be changed.
 */
import Joi from 'joi';

Joi.objectId = require('joi-objectid')(Joi);

// eslint-disable-next-line import/prefer-default-export
export const createLenderInstituteNotes = {
  body: Joi.object().keys({
    content: Joi.string().required(),
    lenderInstitute: Joi.objectId().required(),
  }),
};

export const getLenderInstituteNotes = {
  body: Joi.object().keys({}).unknown(true),
  params: Joi.object().keys({
    lenderInstitute: Joi.objectId().required(),
  }),
  query: Joi.object()
    .keys({
      page: Joi.string(),
      limit: Joi.string(),
      sort: Joi.string(),
      order: Joi.string().valid('asc', 'desc').default('asc'),
    })
    .unknown(true),
};

export const updateLenderInstituteNotes = {
  body: Joi.object().keys({
    content: Joi.string().required(),
    lenderInstitute: Joi.objectId().required(),
  }),
  params: Joi.object().keys({
    lenderInstituteNotesId: Joi.objectId().required(),
  }),
};
export const deleteLenderInstituteNotesById = {
  params: Joi.object().keys({
    lenderInstituteNotesId: Joi.objectId().required(),
  }),
};
